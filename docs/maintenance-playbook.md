# Maintenance Playbook

- Track classifier unknown-rate increases after LinkedIn UI updates.
- Capture new fixtures and update `docs/rule-mapping.md` when selectors drift.
- Add regression tests for every new rule or selector change.
- Keep schema versioning backward-compatible for stored settings.
